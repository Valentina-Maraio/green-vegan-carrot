import React from 'react'
//useEffect(() = > {setTimeout(() => {fetch ...}, 2000); }, []);

const Loading = () => {
    return (
        <div>
            <h3>Loading</h3>
        </div>
    )
}

export default Loading